- name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install pyTelegramBotAPI requests flask pandas numpy ccxt

      - name: Run Crypto Analyzer
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: python tawsiaaat1111.py# تأكد من اسم ملفك هنا (radd22.py أو الاسم الجديد)
